#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp (ql:quickload '(clkedit) :silent t)
  )

(defpackage :ros.script.clkedit
  (:use :cl :clkedit))
(in-package :ros.script.clkedit)

(defun main (&rest argv)
  (declare (ignorable argv))
  (run-server))
;;; vim: set ft=lisp lisp:
